#!/bin/bash
# this file runs the interface with the proper commandline arguments (they are optional, but the hardcoded values only apply to the TUDelft testing environment), being <server IP> <server port>. These may need adaptation to your system.
./interface.out localhost 4547